# NapFit 設計書 v2

## 0. このドキュメントについて

本ドキュメントは、仮眠特化アプリ「NapFit」を Claude Code で実装するための設計書。読者は Claude Code および開発者(発注者)を想定。

実装にあたって判断に迷う箇所が出た場合、本書の方針(シンプルさ優先 / コンセプトへの忠実さ / モックとの整合)を尊重して判断すること。

---

# NapFit 決定事項まとめ

## 1. アプリ名

**NapFit**

読み方：ナップフィット

## 2. サブタイトル

**あなたに合う仮眠時間がわかる**

## 3. アプリの基本コンセプト

普通のアラームアプリではなく、  
**自分に合う仮眠時間を見つけるためのアラーム・昼寝分析アプリ**。

主な価値は、

- 仮眠時間を選ぶ
- アラームで起きる
- 起きた後の気分を記録する
- 記録から、自分に合う仮眠時間が見えてくる

という流れ。

## 4. 採用コピー

夜の睡眠時間に個人差があるように、昼の仮眠にも「すっきり起きやすい長さ」は人それぞれ。

NapFitは、仮眠後の気分を記録しながら、あなたに合う仮眠時間を見つけるためのアラーム・昼寝分析アプリです。

## 5. 訴求の軸

売りは **「アラーム」ではなく「あなたに合う仮眠時間がわかる」**。

普通のアラームとの差別化は、

```text
普通のアラーム：
時間になったら鳴るだけ

NapFit：
時間になったら起こす
起きた気分を記録する
記録から自分に合う仮眠時間が分かるす + 起きた気分を記録 + 記録から自分に合う仮眠時間が分かる |

### 1.4 コア体験

1. アプリを起動
2. 仮眠時間を選ぶ(15分 / 20分 / 30分 / カスタム)
3. 仮眠中(BGMを流しつつカウントダウン)
4. 時間が来たらアラームで起床
5. 起きたらその場で「すっきり / 普通 / だるい」を1タップ記録(スキップ可)
6. 履歴から「自分に合う仮眠時間」が見えてくる

### 1.5 ターゲット

昼休みに仮眠を取りたい日本の社会人。「寝過ごさず、自分にとってベストな仮眠時間を知りたい」人。

---

## 2. 開発・リリース前提

| 項目 | 内容 |
|---|---|
| 開発者環境 | Windows のみ(Mac なし) |
| 実機テスト | iOS 実機あり |
| フレームワーク | Expo(React Native) |
| ビルド/申請 | EAS Build + EAS Submit(Windows で完結) |
| リリース先 | App Store |
| 対応 OS | iOS 15 以上 |
| 対応言語 | 日本語のみ(将来の英語対応に備えて i18n 構造は持つが、初期は日本語のみ実装) |
| Apple Developer Program | 登録必要($99/年) |
| コード実装 | Claude Code に委譲 |
| デザイン | モック作成済み(別途参照) |
| アプリアイコン | 作成済み |

---

## 3. アラーム方式

### 3.1 採用方式: background audio 方式

仮眠中に音声(BGM または極小音量の環境音)を流し続けることで、iOS のバックグラウンドオーディオ権限を維持し、アプリを閉じても・ロックしても再生が継続する仕組み。タイマー満了時にそのオーディオセッション経由でアラーム音を鳴らす。

### 3.2 実装要素

- `expo-av` または `expo-audio`(BGM・アラーム音・バックグラウンド再生)
- `expo-keep-awake`(画面消灯防止)
- `expo-notifications`(保険のローカル通知)
- `Info.plist` の `UIBackgroundModes` に `audio` を宣言

### 3.3 既知の弱点(承知の上で採用)

- 音が完全に止まるとセッションが切れる(他アプリの音割り込み、ユーザーの手動停止など)
- OS がメモリ不足でアプリを落とすと鳴らない
- 「音を流し続ける」が技術的な前提になる

これらの失敗を救済UX(セクション9)でフォローする。

### 3.4 不採用にした方式

- **AlarmKit**(iOS 26 専用)— OS レベルでアラームを保証してくれるが、iOS 26 限定で対応 OS が狭くなる。将来の拡張候補として保留。

---

## 4. 機能構成

### 4.1 無料版

- 15分 / 20分 / 30分タイマー + カスタム時間(5分〜60分、1分刻み)
- BGM(厳選2〜3種)
- アラーム音(1種、フェードイン3秒)
- 起床通知 + アラーム
- 3択記録(すっきり / 普通 / だるい、スキップ可)
- 直近10件の履歴
- 簡単コメント(ホーム画面上部に表示)
- 「あなたのおすすめ仮眠時間」(軽量版、ホーム上部)
- 救済UX

### 4.2 Pro版(買い切り 480円)

- 分析ダッシュボード(時間別 / 曜日別 / 時間帯別のすっきり率)
- 最適仮眠時間の診断レポート(詳細版)
- 無制限履歴 + カレンダー表示
- データエクスポート(CSV / JSON)
- ダーク / ライトモード選択(仮眠中画面にも反映)
- アラーム音の選択(複数種)

### 4.3 採用しないもの

- 広告(なし)
- サブスクリプション(なし)
- スヌーズ機能(コンセプトと矛盾)
- 再通知の間隔・回数設定(同上)
- 寝つき時間の加算(ユーザーが選んだ時間 = 起きるまでの時間)
- テーマ変更・デザインカスタマイズ(ライト/ダーク選択のみ)
- BGM の追加音源(選ぶ手間が増えるため厳選で固定)
- クラウド同期(MVPでは過剰)
- アナリティクス(初期は入れない。プライバシー申告をシンプルに保つ)

---

## 5. 画面構成

### 5.1 ナビゲーション

フッタータブ3つ:**ホーム / 履歴 / 設定**

仮眠中〜起床〜記録の画面群は、タブの上に全画面で重なるモーダル層として扱う(フッターは見えない)。

```
タブ層(フッター常時表示)
  ├─ ホーム
  ├─ 履歴
  └─ 設定

モーダル層(タブの上に全画面で重なる)
  └─ 仮眠中 → 起床+記録 → 閉じてタブ層に戻る
```

### 5.2 画面一覧

#### 5.2.1 スプラッシュ画面

- ロゴ表示の最小限。Expo 標準の splash 機能を使用。
- 初回起動時のみ:オンボーディングへ遷移(後日詳細決定、本書では未定義)。
- 2回目以降:ホームへ直接遷移。

#### 5.2.2 ホーム

**表示要素(上から)**:

1. ヘッダー(ロゴ、メニューアイコン)
2. **「あなたのおすすめ仮眠時間」カード**(無料・Pro 共通、ただし内容は異なる)
   - 記録 5件以上のとき: 数字 + 一言コメント + 簡易グラフ(15/20/30分の比較棒グラフ)
   - 記録 5件未満のとき: 励まし文言のみ表示
   - Pro 版: より詳細な根拠データへのリンク(「診断レポートを見る」)が出る
3. 簡単コメント(セクション6参照)
4. 仮眠時間ボタン(15分 / 20分 / 30分)
5. カスタム設定リンク(タップでカスタム時間設定モーダル)
6. 「仮眠をはじめる」大ボタン(時間選択後にアクティブ化)
7. 救済UXバナー(条件を満たすときのみ、ホーム上部にオーバーレイ表示。セクション9参照)

#### 5.2.3 履歴

**表示要素**:

1. ヘッダー(タイトル「履歴」)
2. **直近10回のまとめ**(無料): すっきり/普通/だるいの回数集計
3. 記録リスト(日付・仮眠時間・3択結果・アイコン)
4. 無料: 直近10件まで。10件目の下に「無制限履歴を見る(Pro)」のCTA
5. Pro: 全件表示 + カレンダー表示への切り替えタブ

**カレンダー表示(Pro)**:

- 月単位表示
- 各日のセルに、その日の代表結果アイコン(複数あれば最良結果を採用)
- 日タップで、その日の全記録をモーダル表示
- 左右矢印で月の切り替え

#### 5.2.4 設定

**表示要素**:

- 通知設定(状態表示:許可 / 未許可。タップで OS の設定画面へ)
- BGM 選択(2〜3種から)
- アラーム音選択(Pro)
- テーマ:ライト/ダーク選択(Pro)
- NapFit Pro 購入(未購入時) / Pro メンバー(購入後)
- 購入を復元(Restore Purchases)
- プライバシーポリシー
- 利用規約
- アプリ情報(バージョン、ライセンス)

#### 5.2.5 仮眠中(モーダル)

**表示要素**:

- 閉じる(×)ボタン(左上)— タップで中断確認ダイアログ
- 「この画面を開いたままにしてください」(画面上部)
- 残り時間(大きな数字、MM:SS 形式)
- 「♪ BGM再生中」インジケーター
- ヘッダー画像(羊のイラスト、テーマに合わせて切り替え)
- 「中断する」ボタン(画面下部)

**挙動**:

- `expo-keep-awake` で画面消灯防止
- BGM 再生中
- テーマ設定に従う(ライトモード設定時もそのテーマで表示、輝度は変更しない)
- 残り時間は**絶対時刻基準で計算**(タイマー開始時刻 + 仮眠時間 - 現在時刻)、`setInterval` の累積誤差を回避する

#### 5.2.6 起床+記録(統合画面、モーダル)

**フェーズ1: アラーム鳴動**

- 「おはようございます!」「起きる時間です」
- ヘッダー画像(羊が目覚めたイラスト)
- 「アラームを止める」大ボタン
- 「スキップ(記録しない)」リンク

**フェーズ2: 3択記録**(「止める」を押した後、同画面で切り替え)

- 「起きた感じを教えてください」
- 「今日のコンディションを記録しましょう」
- 3択ボタン(縦並び、色分け、説明文付き)
  - すっきり(緑): 「よく休めた気がする」
  - 普通(黄): 「ふつう」
  - だるい(赤): 「まだ眠い・重い感じ」
- 下部の補足文言「記録すると、あなたに合う仮眠時間が見えてきます」
- 3択を選択 → ホームに遷移、選択完了のトースト表示

**スキップ時**:

- 「スキップしても、仮眠の記録は残ります」の補足
- スキップ → ホームに遷移、`status='skipped'` で保存

#### 5.2.7 分析ダッシュボード(Pro)

ホームの「あなたのおすすめ仮眠時間」カード、または履歴画面の「もっと見る」から遷移。

**表示要素**:

1. タブ:時間別 / 曜日別 / 時間帯別
2. 選択中タブの棒グラフ(すっきり率を縦軸)
3. **最適仮眠時間カード**: 「あなたの最適仮眠時間(診断)」 + 数字 + 一言コメント
4. 「診断レポートをもっと見る」ボタン(より詳細なテキストレポートへ)

**集計対象**: `status` が `'completed'` または `'recovered'` のレコードのみ。

### 5.3 Pro 訴求の導線

複数箇所に導線を置くが、各1回まで(うるさくならないように)。

| 導線 | タイミング | 表示形式 |
|---|---|---|
| 設定画面 | 常時 | リスト項目「NapFit Pro - 機能をアップグレード」 |
| 履歴10件超過 | 11件目の記録が発生した後、初回履歴アクセス時 | リストの下部にCTAカード |
| 分析機能タップ | 無料ユーザーが「診断レポートをもっと見る」等をタップ | モーダルで Pro 機能の説明 + 購入ボタン |
| Pro限定機能の項目 | 設定画面の該当項目タップ | モーダルで Pro 機能の説明 + 購入ボタン |

---

## 6. 簡単コメント

### 6.1 表示場所

ホーム画面の上部(「あなたのおすすめ仮眠時間」カードの下)。

### 6.2 トーン

事実 + 最小限の文章化。

例:「20分仮眠は5回中4回すっきりです」「最近5回はすっきりが続いています」

### 6.3 発火条件と文言

#### カテゴリ1: 特定時間の成績

| 結果傾向 | 文言例 | 発火条件 |
|---|---|---|
| すっきり多め | 「20分仮眠は5回中4回すっきりです」 | ある仮眠時間で3回以上の記録、かつ特定の結果が60%以上 |
| だるい多め | 「30分仮眠は3回中2回だるい結果でした」 | 同上 |
| 普通多め | 「15分仮眠は4回中3回普通でした」 | 同上 |

#### カテゴリ2: 最近の傾向

| 結果傾向 | 文言例 |
|---|---|
| すっきり続き | 「最近5回はすっきりが続いています」 |
| だるい続き | 「最近5回はだるい結果が多めです」 |
| 普通続き | 「最近5回は普通が多めです」 |

発火条件: 直近5件のうち4件以上が同じ結果。

#### カテゴリ3: ベスト仮眠時間の示唆

文言例: 「これまでで一番すっきりだったのは20分仮眠です」

発火条件: 各時間で2回以上の記録があり、すっきり率に30%以上の差。

#### カテゴリ4: 励まし系(データ不足時)

| 記録数 | 文言例 |
|---|---|
| 0件 | 「仮眠を記録すると、自分に合う時間が分かります」 |
| 1〜2件 | 「記録を続けると、あなたに合う仮眠時間が見えてきます」 |
| 3〜4件 | 「あと◯回記録すると、傾向が見えてきます」 |

### 6.4 表示の優先順位

`カテゴリ4(励まし、データ不足時)` → `カテゴリ2(最近の傾向)` → `カテゴリ3(ベスト時間)` → `カテゴリ1(特定時間の成績)`

複数カテゴリが成立する場合は上位を表示。

### 6.5 集計対象

`status` が `'completed'` または `'recovered'` のレコードのみ。`'skipped'` `'interrupted'` `'in_progress'` は集計から除外。

---

## 7. 「あなたのおすすめ仮眠時間」(ホーム上部カード)

### 7.1 役割

アプリを開いた瞬間に Pro 価値(分析)の片鱗を見せ、Pro 訴求の入口にする。

### 7.2 表示パターン

| 記録数 | 表示内容 | Pro導線 |
|---|---|---|
| 0〜4件 | 励まし文言のみ。「記録を続けると、あなたのおすすめ仮眠時間が表示されます」 | なし |
| 5件以上(無料) | 数字(最もすっきり率の高い時間)+ 一言コメント + 簡易棒グラフ(15/20/30分の比較) | カード下部に「もっと詳しく見る(Pro)」リンク |
| 5件以上(Pro) | 同上 + 「診断レポートを見る」ボタン → 分析ダッシュボードへ | あり |

### 7.3 おすすめ時間の判定ロジック

`completed` + `recovered` のレコードのうち、各仮眠時間(15/20/30/カスタム値ごと)で2件以上の記録があるものを対象に、すっきり率が最も高い時間を選出。

- 該当が複数ある場合: 記録数が多い方を優先
- 全ての時間で2件未満の場合: カテゴリ4(励まし)の表示

---

## 8. データ構造

### 8.1 保存方式

**SQLite**(`expo-sqlite`)を採用。

ユーザー設定(BGM選択、テーマ、Pro購入状態、初回起動フラグ、各種フラグ)は `AsyncStorage` で保存。

### 8.2 テーブル: nap_records

```sql
CREATE TABLE nap_records (
  id TEXT PRIMARY KEY NOT NULL,             -- UUID v4
  started_at TEXT NOT NULL,                 -- ISO 8601 (タイマー開始時刻)
  ended_at TEXT,                            -- ISO 8601 (アラーム停止/中断/救済記録時刻)。in_progress では NULL
  nap_duration_minutes INTEGER NOT NULL,    -- ユーザーが選んだ仮眠時間
  result TEXT,                              -- 'fresh' | 'normal' | 'sluggish' | NULL
  status TEXT NOT NULL,                     -- 'in_progress' | 'completed' | 'skipped' | 'interrupted' | 'recovered'
  created_at TEXT NOT NULL                  -- ISO 8601 (レコード作成時刻、救済UX用)
);

CREATE INDEX idx_started_at ON nap_records(started_at);
CREATE INDEX idx_status ON nap_records(status);
```

### 8.3 TypeScript 型定義

```typescript
type NapResult = 'fresh' | 'normal' | 'sluggish';

type NapStatus =
  | 'in_progress'  // タイマー稼働中
  | 'completed'    // 正常完了(3択記録あり)
  | 'skipped'      // アラームは鳴ったが3択スキップ
  | 'interrupted'  // 仮眠中に中断ボタンで終了
  | 'recovered';   // 救済UX経由で後から記録

interface NapRecord {
  id: string;                  // UUID v4
  startedAt: string;           // ISO 8601
  endedAt: string | null;
  napDurationMinutes: number;
  result: NapResult | null;
  status: NapStatus;
  createdAt: string;
}
```

### 8.4 設計判断の補足

- `dayOfWeek` `timeOfDay` は保存せず、`startedAt` から都度算出。記録数オーダー(数百〜千件)を考えると計算コストは無視できる。
- BGM や アラーム音の種類は保存しない。分析価値が薄い。
- 将来「メモ」「コンディションタグ」を追加する場合は、別テーブル(`nap_record_meta` など)に追加して JOIN する設計。`nap_records` 本体は触らない。
- マイグレーション: スキーマバージョンを `AsyncStorage` に保存。アプリ起動時にバージョン比較してマイグレーションを実行する仕組みを最初から組み込む。

### 8.5 ステータスごとの扱い

| status | 履歴に表示 | 簡単コメント集計 | 分析ダッシュボード集計 | おすすめ時間判定 |
|---|---|---|---|---|
| `in_progress` | しない | しない | しない | しない |
| `completed` | する | する | する | する |
| `skipped` | する(結果欄は「—」) | しない | しない | しない |
| `interrupted` | する(結果欄は「中断」) | しない | しない | しない |
| `recovered` | する | する | する | する |

### 8.6 AsyncStorage に保存するキー

| キー | 内容 | 型 |
|---|---|---|
| `app:schema_version` | DB スキーマバージョン | number |
| `app:onboarding_completed` | オンボーディング完了フラグ | boolean |
| `app:first_launch_at` | 初回起動日時 | string (ISO 8601) |
| `settings:bgm_id` | 選択中の BGM ID | string |
| `settings:alarm_sound_id` | 選択中のアラーム音 ID(Pro) | string |
| `settings:theme` | 'light' / 'dark' / 'system' (Pro) | string |
| `pro:entitlement_active` | Pro 有効フラグ(RevenueCat 同期) | boolean |
| `pro:last_verified_at` | Pro 状態の最終検証日時 | string |
| `ux:pro_cta_shown_history_10` | 履歴10件超過時のCTAを表示済みか | boolean |
| `ux:last_session_app_version` | 最後に起動したアプリバージョン | string |

---

## 9. アラーム動作と救済UXの詳細仕様

### 9.1 通常フロー

1. ユーザーがホームで仮眠時間を選択し「仮眠をはじめる」をタップ
2. レコードを `status='in_progress'` で挿入(`started_at` = 現在時刻)
3. 仮眠中画面に遷移、`expo-keep-awake` で消灯防止、BGM 再生開始
4. ローカル通知を「指定時刻に発火」で予約(保険)
5. 残り時間は **絶対時刻基準** で計算(`started_at + nap_duration_minutes - 現在時刻`)
6. 指定時間が経過 → アプリ内でアラーム音再生(BGMからアラームへフェードイン3秒で切り替え)
7. 起床+記録画面に自動遷移
8. ユーザーが「アラームを止める」をタップ → アラーム停止
9. 同じ画面に3択が表示
10. ユーザーが3択を選択 → レコードを `completed`、`result` を記録、`ended_at` を現在時刻で更新
11. ユーザーがスキップ → レコードを `skipped`、`ended_at` を現在時刻で更新
12. ホームに遷移、トーストで「記録しました」(3択時)または何も表示しない(スキップ時)

### 9.2 中断フロー

1. 仮眠中画面で「中断する」をタップ
2. 確認ダイアログ「仮眠を中断しますか?」「中断」/「続ける」
3. 「中断」選択 → レコードを `interrupted` に更新、`ended_at` を現在時刻、ホームに遷移

### 9.3 アラーム音の仕様

- 鳴動時間: 止められないまま **最大5分** で自動停止
- フェードイン: 3秒で最大音量
- 無料: 1種固定
- Pro: 複数種(具体的なラインナップは後日決定)
- サイレントモードでも鳴る: `expo-av` のオーディオセッションを `playback` カテゴリに設定

### 9.4 BGM の仕様

- 無料・Pro 共通: 厳選2〜3種(具体的なラインナップは後日決定)
- 音量: アラーム音より小さく固定(ユーザー調整不可、シンプルさ優先)
- ループ再生

### 9.5 救済UX

#### 9.5.1 検出ロジック

アプリ起動時(またはバックグラウンドからフォアグラウンドへ復帰時)に以下の条件を満たすレコードを検索:

- `status = 'in_progress'`
- `started_at + nap_duration_minutes + 30分 < 現在時刻`
- `started_at` が **24時間以内**

該当レコードがあれば、ホーム画面上部にバナーを表示。

該当レコードが24時間より前のものは、自動で `interrupted` に変換してバナーは出さない。

#### 9.5.2 バナー表示内容

```
┌────────────────────────────────────┐
│ ◯時間前の◯分仮眠が記録されていません │
│ どうでしたか?                       │
│ [すっきり] [普通] [だるい]           │
│ [スキップ]                          │
└────────────────────────────────────┘
```

#### 9.5.3 ユーザー操作後

- 3択選択 → `status='recovered'`、`result` を記録、`ended_at` と `created_at` を現在時刻で更新、バナー消滅
- スキップ → `status='skipped'`、`ended_at` と `created_at` を現在時刻で更新、バナー消滅

### 9.6 境界ケース・状態遷移

#### 9.6.1 仮眠中に電話がかかってきた

- タイマー継続(時間ベース計算なので影響なし)
- 電話による音声割り込みでオーディオセッションが中断される可能性あり
- 電話終了後、`AVAudioSession` の `interruptionEnded` イベントで BGM を自動再開する実装を入れる
- アラーム時刻が電話中だった場合、電話終了後にアラームを鳴らす(時刻ベースで残り時間を判定)

#### 9.6.2 仮眠中にアプリが完全終了した

- レコードは `in_progress` のまま残る
- 次回起動時に救済UX(セクション 9.5)で検出
- 保険のローカル通知が予約されていれば、終了中でも通知だけは届く

#### 9.6.3 2回目の仮眠を開始しようとしたら、前回が `in_progress` のまま残っていた

- 救済UXのバナーが表示される
- ユーザーが救済操作(3択 or スキップ)を済ませてから新規仮眠を開始させる
- バナーがある間は「仮眠をはじめる」ボタンを無効化(タップ時にバナーへスクロール)

#### 9.6.4 通知許可が拒否された

- アプリは使用可能(画面つけっぱなしならアラームは鳴る)
- オンボーディング時に1回要求、拒否されたら再要求しない
- 設定画面の「通知設定」項目に「通知が許可されていません」を表示
- タップで OS の設定画面に遷移する案内ダイアログを出す

#### 9.6.5 仮眠中にイヤホンを抜いた

- iOS の標準挙動として、ヘッドホン取り外し時には再生が停止する(意図しない大音量再生を防ぐ仕様)
- これに対しては BGM 再開のリトライを試みる
- それでも継続できない場合、保険のローカル通知に頼る

#### 9.6.6 同じ日に複数回仮眠した

- 通常に記録される(問題なし)
- カレンダー表示では、その日のセルに最良結果のアイコンを表示
- 日タップで全記録をモーダル表示

---

## 10. Pro(課金)の実装仕様

### 10.1 課金プラットフォーム

- RevenueCat 経由
- 商品タイプ: Non-Consumable(買い切り)
- 価格: 480円(税込)

### 10.2 識別子

- Product ID: `com.napfit.pro` (仮)
- Entitlement ID: `pro`
- Offering ID: `default`

### 10.3 機能ロックの判定

- アプリ起動時に RevenueCat から Pro 状態を取得し、`AsyncStorage` の `pro:entitlement_active` に保存
- アプリ内では `pro:entitlement_active` を参照して機能のロック判定
- 24時間ごとに RevenueCat と同期(最終検証日時 `pro:last_verified_at` を参照)
- 同期失敗時はキャッシュを信じて動作(オフライン耐性)

### 10.4 ロック判定の対象機能

| 機能 | ロック対象 |
|---|---|
| 履歴の11件目以降 | はい |
| カレンダー表示 | はい |
| 分析ダッシュボード | はい |
| 詳細な診断レポート | はい |
| CSV/JSON エクスポート | はい |
| ダーク/ライトモード選択 | はい |
| アラーム音の選択 | はい |

### 10.5 購入フロー

1. ユーザーが Pro 訴求モーダル または 設定画面の Pro 項目をタップ
2. RevenueCat の paywall または独自 UI で商品情報を表示
3. 「購入する」タップ → iOS のシステム購入ダイアログ
4. 購入成功 → `pro:entitlement_active = true` に更新、トーストで「Pro機能が有効になりました」
5. 購入失敗 → エラーメッセージ表示、再試行可能
6. 「あとで」/「閉じる」でモーダルを閉じる

### 10.6 復元フロー

- 設定画面の「購入を復元」をタップ
- RevenueCat の `restorePurchases()` を呼び出し
- 成功時: Pro 状態を反映、トーストで「購入を復元しました」
- 成功(復元対象なし)時: 「復元できる購入が見つかりませんでした」のダイアログ
- 失敗時: エラーダイアログ + リトライボタン

---

## 11. 技術スタック

| 用途 | ライブラリ |
|---|---|
| フレームワーク | Expo SDK 最新版(React Native) |
| ナビゲーション | Expo Router(タブ + モーダル層) |
| 音声 | `expo-av` または `expo-audio`(BGM・アラーム・バックグラウンド再生) |
| 消灯防止 | `expo-keep-awake` |
| 通知(保険) | `expo-notifications` |
| ローカル DB | `expo-sqlite` |
| 設定保存 | `@react-native-async-storage/async-storage` |
| 課金 | `react-native-purchases`(RevenueCat) |
| 日時操作 | `date-fns` |
| UUID生成 | `expo-crypto` の `randomUUID()` または `uuid` |
| グラフ表示 | `react-native-svg` + 自前実装 または `victory-native`(分析ダッシュボード用) |
| ビルド/申請 | EAS Build + EAS Submit |

---

## 12. プロジェクト構造(提案)

```
napfit/
├── app/                          # Expo Router(画面)
│   ├── (tabs)/                   # タブナビゲーション
│   │   ├── _layout.tsx
│   │   ├── index.tsx             # ホーム
│   │   ├── history.tsx           # 履歴
│   │   └── settings.tsx          # 設定
│   ├── nap/                      # モーダル層
│   │   ├── _layout.tsx
│   │   ├── active.tsx            # 仮眠中
│   │   └── wake.tsx              # 起床+記録
│   ├── analytics.tsx             # 分析ダッシュボード(Pro)
│   └── _layout.tsx               # ルートレイアウト
├── src/
│   ├── db/
│   │   ├── client.ts             # SQLite クライアント
│   │   ├── migrations.ts         # マイグレーション
│   │   └── queries.ts            # クエリ関数
│   ├── audio/
│   │   ├── bgm.ts                # BGM 再生制御
│   │   ├── alarm.ts              # アラーム再生制御
│   │   └── session.ts            # AVAudioSession 設定
│   ├── nap/
│   │   ├── timer.ts              # 絶対時刻ベースタイマー
│   │   ├── recovery.ts           # 救済UX検出
│   │   └── statistics.ts         # 統計集計
│   ├── comments/
│   │   └── generator.ts          # 簡単コメント生成ロジック
│   ├── pro/
│   │   ├── revenuecat.ts         # RevenueCat 連携
│   │   └── gate.ts               # 機能ロック判定
│   ├── notifications/
│   │   └── schedule.ts           # 保険通知の予約
│   ├── components/               # 共通 UI コンポーネント
│   ├── theme/
│   │   ├── colors.ts             # カラーパレット
│   │   └── typography.ts
│   ├── i18n/
│   │   └── ja.ts                 # 日本語文言(将来の国際化対応)
│   └── utils/
├── assets/
│   ├── audio/
│   │   ├── bgm/                  # BGM 音源
│   │   └── alarm/                # アラーム音源
│   ├── images/                   # キャラ画像など
│   └── icon.png
├── app.json                      # Expo 設定
├── eas.json                      # EAS Build 設定
└── package.json
```

---

## 13. App Store 申請に必要な情報

### 13.1 アプリ情報

- アプリ名: NapFit
- サブタイトル: あなたに合う仮眠時間がわかる
- カテゴリ:
  - プライマリ: ヘルスケア/フィットネス
  - セカンダリ: ライフスタイル
- 年齢制限: 4+
- 価格: 無料(アプリ内課金あり)

### 13.2 App Privacy 申告(初期構成)

- データ収集なし(全てローカル保存、アナリティクスなし)
- App Privacy セクションで「データを収集しない」を選択
- ※将来アナリティクスを追加する場合は再申告

### 13.3 アプリ内課金

- NapFit Pro(Non-Consumable、480円)

### 13.4 必須準備物

- アプリアイコン(1024x1024、作成済み)
- スクリーンショット
  - 6.7インチ iPhone:5枚以上
  - 6.5インチ iPhone:5枚以上(または 6.7 を流用可能)
  - 5.5インチ iPhone:任意
- App Store 説明文(日本語):
  - キャッチコピー(採用済み): 「夜の睡眠時間に個人差があるように、昼の仮眠にも『すっきり起きやすい長さ』は人それぞれ。NapFitは、仮眠後の気分を記録しながら、あなたに合う仮眠時間を見つけるためのアラーム・昼寝分析アプリです。」
  - 機能一覧
  - サポート URL
  - マーケティング URL(任意)
- プライバシーポリシー URL(必須)
- 利用規約 URL(推奨)

---

## 14. 後日決定する項目

- オンボーディングフロー(初回起動時の説明、3〜4画面想定)
- BGM 音源の具体的なラインナップ(2〜3種、ライセンスクリアな音源を選定)
- アラーム音の具体的なラインナップ(無料1種、Pro 3〜5種程度)
- スクリーンショット(撮影)
- プライバシーポリシー・利用規約の文面
- サポート URL の運用方法(問い合わせメール or 静的ページ)
- アプリ内ヘルプの有無(MVP では不要、ユーザーからの要望次第で追加)

---

## 15. 実装の優先順位(MVP → リリース)

リリース可能な最小構成を最優先で実装し、その後拡張する順番。

### Phase 1: MVP(無料機能のみ完動)

1. プロジェクト初期化(Expo + Expo Router + 必要ライブラリ)
2. SQLite セットアップ + マイグレーション
3. ホーム画面(時間選択、「仮眠をはじめる」)
4. 仮眠中画面(タイマー、BGM 再生、画面消灯防止)
5. 起床+記録画面(アラーム鳴動、3択 + スキップ)
6. 履歴画面(リスト表示)
7. 設定画面(BGM選択、通知設定、プライバシー/利用規約)
8. 救済UX
9. 簡単コメント
10. 「おすすめ仮眠時間」(無料版の軽量表示)

### Phase 2: Pro 機能

11. RevenueCat 統合 + 機能ロック判定
12. 設定画面の Pro 関連項目
13. ダーク/ライトモード
14. アラーム音選択
15. 無制限履歴 + カレンダー表示
16. 分析ダッシュボード(時間別/曜日別/時間帯別)
17. 詳細な診断レポート
18. CSV/JSON エクスポート
19. Pro 訴求モーダル各種

### Phase 3: 申請準備

20. オンボーディング
21. スクリーンショット作成
22. App Store Connect 登録 + 申請

---

## 16. 開発時の判断方針

実装中に本書に記載のない判断が必要になった場合の指針:

1. **シンプルさを優先**:設定項目、画面要素、ユーザーが選ぶべき選択肢は、増やすより減らす方向で判断する。
2. **コンセプトへの忠実さ**:「仮眠の失敗を減らす」「自分に合う仮眠時間の発見」というコンセプトに沿うか確認する。沿わない機能は削る。
3. **モックとの整合**:UI の細部はモックの方向性を尊重する。羊キャラ・色使い・レイアウトの雰囲気を守る。
4. **既存ライブラリを使う**:車輪の再発明をしない。Expo SDK / よく使われるサードパーティライブラリを優先。
5. **失敗を想定する**:アラーム方式の弱点を踏まえ、状態遷移・エラー時の挙動を必ず設計する。「とりあえず動く」で済ませない。
6. **判断に迷うものは記録する**:本書に明記されておらず、かつ重要な判断は、コード内コメント または README に判断理由を残す。
